== Bukkit Keeper

This is a webapp which parses uploaded log files from Amazon's s3 storage service.  It generates a report of usage for each client and associated costs.  It is intended to be used as part of a larger billing system.
